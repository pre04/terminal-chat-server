; Use modern omit=dev instead of deprecated production=true
; This replaces NPM_CONFIG_PRODUCTION=true that deployment platforms may set,
; suppressing the warning: "npm warn config production Use --omit=dev instead."
omit=dev
